#!/usr/bin/env sh

# Ensure the project builds (non-blocking until turbo/SWC issue is resolved)
npm run build || echo "⚠️  Build failed - please fix before merging"

# TODO: Uncomment when tests are added
# npm test
